<section class="page-width page-template">
  <span class="kicker">AntSport</span>
  <h1 class="product-title">{{ page.title }}</h1>
  <div class="lead">{{ page.content }}</div>
</section>

{% schema %}
{ "name": "Pagina", "settings": [] }
{% endschema %}
